#Setup for the query tests. 

--let $no_of_runs = 10
--let $file_name = query_timing_results.txt

# Checked that the performance_schema is setup correctly.;


--disable_query_log
--disable_result_log

UPDATE performance_schema.setup_instruments
       SET ENABLED = 'YES', TIMED = 'YES'
       WHERE NAME LIKE '%statement/%';
UPDATE performance_schema.setup_instruments
       SET ENABLED = 'YES', TIMED = 'YES'
       WHERE NAME LIKE '%stage/%';
UPDATE performance_schema.setup_consumers
       SET ENABLED = 'YES'
       WHERE NAME LIKE '%events_statements_%';
UPDATE performance_schema.setup_consumers
       SET ENABLED = 'YES'
       WHERE NAME LIKE '%events_stages_%';



--echo #
--echo #Setup the tables and import the standard data for queries to run
--echo #
--source suite/histogram_plugin/t/setup_tables.test
--source suite/histogram_plugin/t/populate_tables.test

--enable_query_log
--enable_result_log

